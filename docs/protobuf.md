# Protobuf

Source contract: `proto/robot_telemetry.proto`.

C code generated by protobuf-c: `generated/protobuf-c/robot_telemetry.pb-c.c` and `generated/protobuf-c/robot_telemetry.pb-c.h`.

Recommended evolution rules:

- Never reuse numbers from removed fields.
- Add new fields in a compatible way.
- Keep the package versioned; currently `robot.v1`.
- Document schema changes in this file.

To regenerate:

```bash
make proto
```
